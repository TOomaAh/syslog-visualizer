# Backend API URL for local development
# Used by Next.js rewrites to proxy /api/* requests to the Go backend
# Only needed for local development (not Docker - nginx handles it)
# Default: http://localhost:8080
API_URL=http://localhost:8080
